@model IEnumerable<MvcCoreAWSPostgres.Models.Departamento>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Dept_no)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Dnombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Loc)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Dept_no)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Dnombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Loc)
            </td>
            <td>
                
                @Html.ActionLink("Details", "Details", new {  id=item.Dept_no}) 
            </td>
        </tr>
}
    </tbody>
</table>
